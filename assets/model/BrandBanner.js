export class BrandBanner {
  constructor(background, logo, title, products) {
    this.background = background; // ảnh nền
    this.logo = logo; // ảnh logo thương hiệu
    this.title = title; // mô tả / slogan
    this.path = path; // danh sách sản phẩm thuộc thương hiệu
  }
}